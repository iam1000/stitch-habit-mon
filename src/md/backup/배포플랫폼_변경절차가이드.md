# 배포 플랫폼 변경 절차 가이드

> **목적**: Netlify 크레딧 한도 초과 시 무료 대체 플랫폼으로 쉽게 이전하기  
> **대상**: 배포 지식이 없는 초보자도 따라할 수 있는 수준  
> **작성일**: 2026-02-11

---

## 📋 목차
1. [플랫폼 비교](#1-플랫폼-비교)
2. [추천 플랫폼: Vercel](#2-추천-플랫폼-vercel)
3. [Vercel 배포 단계별 가이드](#3-vercel-배포-단계별-가이드)
4. [대안: Cloudflare Pages](#4-대안-cloudflare-pages)
5. [마이그레이션 체크리스트](#5-마이그레이션-체크리스트)
6. [문제 해결](#6-문제-해결)

---

## 1. 플랫폼 비교

### 무료 플랜 한도 비교표

| 항목 | Netlify | **Vercel** (추천) | Cloudflare Pages | Render |
|------|---------|------------------|------------------|--------|
| **빌드 시간** | 300분/월 | 6,000분/월 | 무제한 | 500시간/월 |
| **대역폭** | 100GB/월 | 100GB/월 | 무제한 | 100GB/월 |
| **함수 실행** | 125,000회 | 100만 요청/월 | 10만 요청/일 | N/A |
| **프로젝트 수** | 500개 | 무제한 | 무제한 | 무제한 |
| **난이도** | ⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ |

### 플랫폼별 장단점

#### ✅ Vercel (가장 추천)
**장점**:
- Netlify와 거의 동일한 사용법 (마이그레이션 쉬움)
- 무료 플랜 한도가 20배 많음 (빌드 시간)
- Serverless Functions 지원 (Netlify Functions와 유사)
- 자동 HTTPS, CDN, 환경 변수 관리 모두 지원
- Next.js 개발사라 React 프로젝트에 최적화

**단점**:
- 함수 실행 시간 제한 (무료: 10초, Free Hobby: 60초)

#### ⚡ Cloudflare Pages
**장점**:
- 대역폭, 빌드 시간 **완전 무제한**
- 전 세계 최고 속도 CDN
- Functions 지원 (Workers 기반)

**단점**:
- Functions 문법이 Netlify와 약간 다름 (변환 필요)
- 설정이 약간 복잡

---

## 2. 추천 플랫폼: Vercel

### 왜 Vercel인가?
1. **마이그레이션이 가장 쉽습니다**: Netlify와 거의 동일한 구조
2. **무료 한도가 넉넉합니다**: 빌드 6,000분/월 (Netlify의 20배)
3. **Serverless Functions 지원**: `netlify/functions` → `api` 폴더로만 옮기면 됨
4. **한국어 가이드 풍부**: 공식 문서와 커뮤니티 활성화

---

## 3. Vercel 배포 단계별 가이드

### 🎯 전체 절차 요약
```
1. Vercel 계정 생성 (Github 연동)
2. 프로젝트 코드 수정 (Functions 경로 변경)
3. Vercel에 프로젝트 Import
4. 환경 변수 설정
5. 배포 완료!
```

---

### Step 1: Vercel 계정 생성

#### 1-1. Vercel 웹사이트 접속
- 주소: [https://vercel.com](https://vercel.com)
- 우측 상단 **"Sign Up"** 클릭

#### 1-2. Github 계정으로 가입
1. **"Continue with GitHub"** 클릭
2. Github 인증 팝업에서 **"Authorize Vercel"** 클릭
3. 가입 완료! (이메일 인증 불필요)

---

### Step 2: Functions 코드 수정 (중요!)

Netlify Functions와 Vercel Functions는 폴더 구조가 다릅니다.

#### 2-1. Functions 폴더 이름 변경

**Windows PowerShell 명령어**:
```powershell
# 현재 프로젝트 폴더로 이동
cd c:\AI_DEV\stitch

# netlify/functions → api 폴더로 복사
Copy-Item -Path "netlify\functions" -Destination "api" -Recurse

# 확인
dir api
```

#### 2-2. 각 Function 파일 수정

Vercel은 파일명이 곧 API 경로가 됩니다.

**예시: `sheets-data.js` 파일**

**변경 전 (Netlify)**:
```javascript
export const handler = async (event, context) => {
  // ... 코드
  return {
    statusCode: 200,
    headers: { ... },
    body: JSON.stringify({ data })
  };
};
```

**변경 후 (Vercel)**:
```javascript
export default async function handler(req, res) {
  // CORS 설정
  res.setHeader('Access-Control-Allow-Origin', '*');
  res.setHeader('Access-Control-Allow-Methods', 'POST, OPTIONS');
  res.setHeader('Access-Control-Allow-Headers', 'Content-Type');
  
  if (req.method === 'OPTIONS') {
    return res.status(200).end();
  }
  
  if (req.method !== 'POST') {
    return res.status(405).json({ error: 'Method Not Allowed' });
  }

  try {
    const { sheetId, clientEmail, privateKey, filters, sheetName } = req.body;
    
    // ... 기존 로직 동일 ...
    
    return res.status(200).json({ data });
  } catch (error) {
    console.error('Error:', error);
    return res.status(500).json({ error: error.message });
  }
}
```

**주요 변경 사항**:
1. `export const handler` → `export default async function handler`
2. `(event, context)` → `(req, res)`
3. `event.body` → `req.body` (자동 파싱됨!)
4. `event.httpMethod` → `req.method`
5. `return { statusCode, body }` → `res.status().json()`

#### 2-3. 모든 Functions 파일 변환

다음 파일들을 위 방식으로 수정하세요:
- ✅ `api/sheets-data.js`
- ✅ `api/sheets-add.js`
- ✅ `api/sheets-update.js`
- ✅ `api/sheets-delete.js`

**변환 템플릿** (복사해서 사용):
```javascript
import { GoogleSpreadsheet } from 'google-spreadsheet';
import { JWT } from 'google-auth-library';

export default async function handler(req, res) {
  // CORS 설정
  res.setHeader('Access-Control-Allow-Origin', '*');
  res.setHeader('Access-Control-Allow-Methods', 'POST, OPTIONS, PUT, DELETE');
  res.setHeader('Access-Control-Allow-Headers', 'Content-Type');
  
  if (req.method === 'OPTIONS') {
    return res.status(200).end();
  }

  try {
    // req.body에서 데이터 가져오기 (자동 파싱됨)
    const { sheetId, clientEmail, privateKey, /* 필요한 필드 */ } = req.body;
    
    // 기존 Netlify 로직 그대로 사용
    // ...
    
    return res.status(200).json({ /* 결과 */ });
  } catch (error) {
    console.error('Error:', error);
    return res.status(500).json({ error: error.message });
  }
}
```

---

### Step 3: Frontend 코드 수정 (API 경로)

#### 3-1. API 호출 경로 변경

**모든 API 호출 코드에서 `/.netlify/functions/` → `/api/` 로 변경**

**예시: `src/hooks/useInvestmentData.js`**

**변경 전**:
```javascript
const apiUrl = import.meta.env.DEV
  ? 'http://localhost:3001/api/sheets/data'
  : '/.netlify/functions/sheets-data';  // ❌ Netlify 경로
```

**변경 후**:
```javascript
const apiUrl = import.meta.env.DEV
  ? 'http://localhost:3001/api/sheets/data'
  : '/api/sheets-data';  // ✅ Vercel 경로
```

#### 3-2. 수정 대상 파일 목록

다음 파일들에서 `/.netlify/functions/` → `/api/`로 일괄 변경:
- `src/hooks/useInvestmentData.js`
- `src/Investment.jsx`
- `src/Dashboard.jsx` (혹시 있다면)
- 기타 API 호출하는 모든 파일

**VSCode 일괄 변경 방법**:
1. `Ctrl + Shift + F` (전체 검색)
2. 검색어: `/.netlify/functions/`
3. 바꿀 내용: `/api/`
4. `Ctrl + Shift + H` (전체 바꾸기)
5. **"Replace All"** 클릭

---

### Step 4: Vercel에 프로젝트 Import

#### 4-1. Github에 코드 푸시
```powershell
cd c:\AI_DEV\stitch
git add .
git commit -m "Migrate to Vercel"
git push origin main
```

#### 4-2. Vercel 대시보드에서 Import

1. Vercel 대시보드 접속: [https://vercel.com/dashboard](https://vercel.com/dashboard)
2. **"Add New..."** → **"Project"** 클릭
3. **"Import Git Repository"** 선택
4. Github 저장소 목록에서 `stitch` 검색 후 **"Import"** 클릭

#### 4-3. 프로젝트 설정

**Configure Project** 화면에서:

| 항목 | 설정값 |
|------|--------|
| **Project Name** | `habitmons` (원하는 이름) |
| **Framework Preset** | Vite (자동 감지됨) |
| **Root Directory** | `./` (기본값) |
| **Build Command** | `npm run build` (자동) |
| **Output Directory** | `dist` (자동) |

**"Deploy"** 버튼 클릭 ✅

---

### Step 5: 환경 변수 설정

#### 5-1. 배포 실패 시 (환경 변수 누락)
첫 배포는 환경 변수가 없어서 실패할 수 있습니다. 정상입니다!

#### 5-2. 환경 변수 추가

1. Vercel 프로젝트 페이지 → **"Settings"** 탭 클릭
2. 왼쪽 메뉴 → **"Environment Variables"** 클릭
3. 다음 변수들을 **하나씩** 추가:

**추가할 환경 변수 목록** (`.env` 파일 내용 그대로):

```
Name: VITE_SUPABASE_URL
Value: (당신의 Supabase URL)
Environments: Production, Preview, Development 모두 체크
---
Name: VITE_SUPABASE_ANON_KEY
Value: (당신의 Supabase Anon Key)
Environments: Production, Preview, Development 모두 체크
---
Name: VITE_GOOGLE_SERVICE_ACCOUNT_EMAIL
Value: (당신의 Google Service Account Email)
Environments: Production, Preview, Development 모두 체크
---
Name: VITE_GOOGLE_SERVICE_ACCOUNT_PRIVATE_KEY
Value: (당신의 Private Key - 그대로 복사)
Environments: Production, Preview, Development 모두 체크
---
Name: VITE_GOOGLE_SHEET_CONFIG
Value: (당신의 Sheet Config JSON - 따옴표 제외)
Environments: Production, Preview, Development 모두 체크
---
Name: VITE_DATA_SHEET_ID
Value: (당신의 Data Sheet ID)
Environments: Production, Preview, Development 모두 체크
---
Name: VITE_AUTH_SHEET_ID
Value: (당신의 Auth Sheet ID)
Environments: Production, Preview, Development 모두 체크
```

**⚠️ 주의사항**:
- `VITE_GOOGLE_SHEET_CONFIG` 값 입력 시 **바깥쪽 따옴표(`'`)는 제외**하고 순수 JSON만 입력
  - ❌ 잘못된 예: `'[{"id":"INVESTMENT",...}]'`
  - ✅ 올바른 예: `[{"id":"INVESTMENT",...}]`

#### 5-3. 재배포

환경 변수 추가 후:
1. 상단 메뉴 → **"Deployments"** 탭 클릭
2. 가장 최근 배포 우측 **"..."** 메뉴 → **"Redeploy"** 클릭
3. **"Redeploy"** 버튼 한 번 더 확인

---

### Step 6: 배포 완료 확인

#### 6-1. 배포 성공 확인
- 상태가 **"Ready"**로 변경되면 성공!
- 보통 1-3분 소요

#### 6-2. 사이트 접속
- 제공된 URL 클릭 (예: `https://habitmons.vercel.app`)
- 로그인 테스트
- Dashboard 데이터 로드 확인

#### 6-3. 커스텀 도메인 (선택사항)
- Settings → Domains → 도메인 추가 가능
- 무료 `.vercel.app` 도메인도 완전히 사용 가능!

---

## 4. 대안: Cloudflare Pages

Vercel이 안 맞거나 더 많은 무료 리소스가 필요하다면 Cloudflare Pages를 사용하세요.

### Cloudflare Pages 특징
- ✅ 빌드 시간, 대역폭 **완전 무제한**
- ✅ Functions 10만 요청/일 (Vercel보다 많음)
- ⚠️ Functions 문법이 Workers 기반이라 변환 복잡

### 간단 절차

#### Step 1: Functions 변환 (복잡)

Cloudflare는 Workers 문법을 사용합니다.

**예시: `functions/sheets-data.js`**
```javascript
export async function onRequestPost(context) {
  const { request } = context;
  const body = await request.json();
  
  try {
    const { sheetId, clientEmail, privateKey } = body;
    // ... 로직 ...
    
    return new Response(JSON.stringify({ data }), {
      status: 200,
      headers: {
        'Content-Type': 'application/json',
        'Access-Control-Allow-Origin': '*'
      }
    });
  } catch (error) {
    return new Response(JSON.stringify({ error: error.message }), {
      status: 500
    });
  }
}
```

**너무 복잡하다면 Vercel 추천!**

#### Step 2: Cloudflare Pages 배포
1. [https://dash.cloudflare.com](https://dash.cloudflare.com) 가입
2. Workers & Pages → Create application → Pages
3. Github 연동 후 저장소 선택
4. Build settings:
   - Framework: Vite
   - Build command: `npm run build`
   - Output: `dist`
5. 환경 변수 추가 (Settings → Environment variables)
6. Deploy!

---

## 5. 마이그레이션 체크리스트

배포 전 반드시 확인하세요!

### ✅ 코드 수정 사항
- [ ] `netlify/functions` → `api` 폴더 복사 완료
- [ ] 모든 Function 파일 Vercel 문법으로 변환
- [ ] Frontend API 경로 변경 (`/.netlify/functions/` → `/api/`)
- [ ] `.env` 파일 내용 확인 (모든 필수 변수 포함)

### ✅ Github 푸시
- [ ] `git add .` 완료
- [ ] `git commit -m "Migrate to Vercel"` 완료
- [ ] `git push origin main` 완료

### ✅ Vercel 설정
- [ ] Vercel 계정 생성 (Github 연동)
- [ ] 프로젝트 Import 완료
- [ ] 환경 변수 7개 모두 추가
- [ ] Redeploy 실행

### ✅ 배포 확인
- [ ] 빌드 성공 (Status: Ready)
- [ ] 사이트 접속 확인
- [ ] 로그인 테스트
- [ ] Dashboard 데이터 로드 확인
- [ ] Investment 추가/수정/삭제 테스트

---

## 6. 문제 해결

### Q1. 빌드가 실패합니다 (Build Failed)
**원인**: 의존성 설치 오류 또는 환경 변수 누락

**해결**:
1. Vercel 대시보드 → Deployments → 실패한 배포 클릭
2. Build Logs 확인
3. 오류 메시지에서 누락된 패키지 확인
4. `package.json`에 해당 패키지가 있는지 확인
5. Github에 푸시 후 Redeploy

### Q2. API 호출이 안 됩니다 (404 Not Found)
**원인**: Functions 경로 오류

**확인사항**:
- [ ] `api` 폴더가 프로젝트 루트에 있는가?
- [ ] Frontend에서 `/api/sheets-data` 경로로 호출하는가?
- [ ] Function 파일에 `export default` 있는가?

**테스트 방법**:
```javascript
// 브라우저 Console에서 테스트
fetch('/api/sheets-data', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({ test: true })
})
.then(r => r.json())
.then(console.log)
```

### Q3. CORS 오류가 발생합니다
**원인**: Function에서 CORS 헤더 누락

**해결**:
모든 Function 파일 상단에 다음 코드 추가:
```javascript
res.setHeader('Access-Control-Allow-Origin', '*');
res.setHeader('Access-Control-Allow-Methods', 'POST, OPTIONS, PUT, DELETE');
res.setHeader('Access-Control-Allow-Headers', 'Content-Type');

if (req.method === 'OPTIONS') {
  return res.status(200).end();
}
```

### Q4. Google Sheets 인증 오류 (401)
**원인**: Private Key 환경 변수 설정 오류

**해결**:
1. Vercel → Settings → Environment Variables
2. `VITE_GOOGLE_SERVICE_ACCOUNT_PRIVATE_KEY` 확인
3. 값에 `\n` (역슬래시+n)이 문자열로 포함되어 있어야 함
4. 따옴표 없이 순수 값만 입력했는지 확인
5. 재배포

### Q5. 로컬 개발 서버가 안 됩니다
**원인**: Vercel CLI 미설치 또는 API 경로 오류

**해결**:
```powershell
# Vercel CLI 설치
npm i -g vercel

# 프로젝트 폴더에서 실행
cd c:\AI_DEV\stitch
vercel dev

# localhost:3000 접속
```

**또는** 기존 `server.js` 사용:
1. `api` 폴더의 함수들을 `server.js`에서도 처리하도록 라우트 추가
2. `npm run dev:all` 계속 사용

---

## 📚 추가 자료

### Vercel 공식 문서
- Vercel Functions: [https://vercel.com/docs/functions](https://vercel.com/docs/functions)
- 환경 변수: [https://vercel.com/docs/environment-variables](https://vercel.com/docs/environment-variables)

### 비교 참고
- Netlify vs Vercel: [https://vercel.com/blog/netlify-to-vercel](https://vercel.com/blog/netlify-to-vercel)

---

## 🎉 완료!

이제 Netlify 크레딧 한도 걱정 없이 **Vercel의 관대한 무료 플랜**으로 배포를 계속할 수 있습니다!

**다음 단계**:
1. 기존 Netlify 프로젝트 삭제 (선택사항)
2. DNS/도메인을 Vercel로 변경 (커스텀 도메인 사용 시)
3. 팀원들에게 새 URL 공유

**궁금한 점이 있다면** Vercel 커뮤니티나 Discord에서 질문하세요!
